% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.R
\name{forest}
\alias{forest}
\alias{forest.cdmeta}
\title{Forest plot for cdmeta objects}
\usage{
forest(x, ...)

\method{forest}{cdmeta}(
  x,
  slab = NULL,
  order = c("none", "increasing", "decreasing", "precision", "weight"),
  level = NULL,
  summary_stat = c("mean", "median"),
  refline = 0,
  xlab = "Effect size",
  main = NULL,
  atransf = NULL,
  at = NULL,
  alim = NULL,
  xlim = NULL,
  digits = 2,
  ci_digits = digits,
  weight_digits = 1,
  show_weights = TRUE,
  show_pi = TRUE,
  show_het = TRUE,
  header = TRUE,
  annotate = TRUE,
  cex = 0.85,
  psize = NULL,
  pch = 22,
  box_col = "black",
  box_bg = "white",
  ci_col = "black",
  summary_col = "black",
  summary_bg = "gray20",
  pi_col = "gray40",
  refline_col = "gray70",
  grid = TRUE,
  grid_col = "gray90",
  qtype = 8,
  symmetric_shapes = TRUE,
  mark_summary_estimate = FALSE,
  mark_prediction_estimate = FALSE,
  estimate_mark_col = "black",
  estimate_mark_lwd = 1,
  mar = c(4.5, 1, 3, 1),
  ...
)
}
\arguments{
\item{x}{An object of class \code{"cdmeta"}.}

\item{...}{Additional arguments passed to \code{graphics::plot()}.}

\item{slab}{An optional character vector of study labels. If \code{NULL},
the names of \code{x$data$y} are used when available; otherwise generic
study labels are created.}

\item{order}{A character string specifying the order of studies in the plot.
Available options are \code{"none"}, \code{"increasing"},
\code{"decreasing"}, \code{"precision"}, and \code{"weight"}.}

\item{level}{Confidence level used for study-specific and summary intervals.
If \code{NULL}, \code{1 - x$alpha} is used.}

\item{summary_stat}{Whether Monte Carlo means (\code{"mean"}) or medians
(\code{"median"}) are used as point estimates for summary rows.}

\item{refline}{Numeric location of the reference line.}

\item{xlab}{Label for the x-axis.}

\item{main}{Optional main title.}

\item{atransf}{Optional function used to transform axis labels and printed
estimates while plotting coordinates remain on the original analysis scale.}

\item{at}{Optional numeric vector specifying tick-mark locations on the
original analysis scale.}

\item{alim}{Optional numeric vector of length 2 specifying plotting limits
for the effect-size axis on the original analysis scale.}

\item{xlim}{Optional numeric vector of length 2 specifying full plotting
limits including the text columns.}

\item{digits}{Number of digits used for axis labels.}

\item{ci_digits}{Number of digits used for effect estimates and intervals.}

\item{weight_digits}{Number of digits used for study weights.}

\item{show_weights}{Logical indicating whether study weights are shown.}

\item{show_pi}{Logical indicating whether the prediction interval is shown.}

\item{show_het}{Logical indicating whether heterogeneity statistics are shown.}

\item{header}{Logical indicating whether column headers are shown.}

\item{annotate}{Logical indicating whether estimates and intervals are printed
on the right side of the plot.}

\item{cex}{Character expansion factor.}

\item{psize}{Optional point sizes for study-specific estimates. If
\code{NULL}, point sizes are proportional to random-effects inverse-variance
weights.}

\item{pch}{Plotting symbol for study-specific estimates.}

\item{box_col}{Border color of study-specific points.}

\item{box_bg}{Fill color of study-specific points.}

\item{ci_col}{Color of study-specific confidence intervals.}

\item{summary_col}{Border color of the summary diamond.}

\item{summary_bg}{Fill color of the summary diamond.}

\item{pi_col}{Color of the prediction interval.}

\item{refline_col}{Color of the reference line.}

\item{grid}{Logical indicating whether vertical grid lines are drawn.}

\item{grid_col}{Color of the grid lines.}

\item{qtype}{Quantile type used in \code{stats::quantile()}. Default is 8.}

\item{symmetric_shapes}{Logical indicating whether the summary diamond and
prediction-interval marker are drawn symmetrically around interval midpoints.}

\item{mark_summary_estimate}{Logical indicating whether the actual summary
point estimate is marked inside the summary diamond.}

\item{mark_prediction_estimate}{Logical indicating whether the actual
predictive point estimate is marked on the prediction-interval row.}

\item{estimate_mark_col}{Color of the actual-estimate marks.}

\item{estimate_mark_lwd}{Line width of the actual-estimate marks.}

\item{mar}{Plot margins passed to \code{graphics::par()}.}
}
\value{
Invisibly returns a data frame containing the plotted study-specific
  estimates, standard errors, confidence intervals, and weights. Attributes
  contain the overall, prediction, and heterogeneity summaries on the original
  analysis scale.
}
\description{
Draws a forest plot from an object of class \code{"cdmeta"}. The plot shows
study-specific effect estimates and confidence intervals, the
confidence-distribution-based summary estimate for the overall mean effect,
and optionally the prediction interval for the effect in a future study.
}
\details{
Study-specific confidence intervals are calculated using the normal
approximation
\deqn{y_i \pm z_{1-\alpha/2} se_i,}
where \eqn{\alpha = 1 - level}. The summary interval for \eqn{\mu} and the
prediction interval are calculated from the Monte Carlo samples in the
\code{cdmeta} object when available.

If the object was created with \code{transf = exp}, \code{forest()} uses
this transformation automatically when \code{atransf = NULL}. Tick marks
supplied through \code{at} remain on the original analysis scale.
}
\examples{
data(hf_iron)
fit_hf <- cdmeta(
  y = hf_iron$yi,
  se = hf_iron$sei,
  B = 10000,
  seed = 11111,
  transf = exp,
  transf_name = "exp"
)

forest(
  fit_hf,
  slab = hf_iron$study,
  at = log(c(0.25, 0.5, 1, 2, 4)),
  xlab = "Risk ratio",
  mark_summary_estimate = TRUE,
  mark_prediction_estimate = TRUE
)

}
\seealso{
\code{\link{cdmeta}}
}
